let grid
function setup(){
  createCanvas(400, 400)
  grid = new Array(4)
  grid = grid.fill().map(x=>[0,0,0,0])
  console.table(grid)
}

function addNumber() {
  let options = []
  for (o of options) {

  }
}


function draw(){
  background(255,200,0)
}